[[okhttplibrary_installation]]
== Using OkHttp

(((OkHttp)))


If you are using Maven or Gradle as build system, you can simply add a dependency to:

* group ID `com.squareup.okhttp`
* artifactId `okhttp`
* version 2.5.0 

NOTE: The version might change over time.


For example, to use OkHttp in a Gradle build add the following dependency to your Gradle build. 

[source,java]
----
include::res/install/build.gradle[]
----
