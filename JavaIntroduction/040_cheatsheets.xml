<section id="cheatsheets" role="wrapper">
	<title>Cheat Sheets</title>
	<para>
		The following can be used as a reference for certain task which
		you have to do.
	</para>
	<section id="cheatsheets_class">
		<title>Working with classes</title>
		<para>
			While programming Java you have to create several classes,
			methods, instance variables. The following uses the package test.
		</para>
		<para>
			<table frame='all'>
				<tgroup cols='2' align='left' colsep='1' rowsep='1'>
					<colspec colname='c1' colwidth="1*" />
					<colspec colname='c2' colwidth="3*" />
					<thead>
						<row>
							<entry>What to do</entry>
							<entry>How to do it</entry>
						</row>
					</thead>

					<tbody>
						<row>
							<entry>
								Create a new class called
								<wordasword>MyNewClass</wordasword>.
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass1.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a new attribute (instance variable) called
								<wordasword>var1</wordasword>
								of type <code>String</code>
								in the
								<code>MyNewClass</code>
								class
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass2.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a Constructor for your
								<code>MyNewClass</code>
								class which has a
								<code>String</code>
								parameter and assigns the value of it to
								the
								<code>var1</code>
								instance
								variable.
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass3.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a new method called
								<wordasword>doSomeThing</wordasword>
								in your class which does not
								return a value and has no parameters.
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass3a.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a new method called
								<wordasword>doSomeThing2</wordasword>
								in your class which does
								not
								return a value and has two parameters,
								an <code>int</code>
								and a <code>Person</code>
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass4.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a new method called
								<wordasword>doSomeThing2</wordasword>
								in your class which
								returns an <code>int</code> value and has three parameters,
								two <code>Strings</code> and a <code>Person</code>.
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyNewClass5.java" />
									</programlisting>
								</para>
							</entry>
						</row>

						<row>
							<entry>
								Create a class called <wordasword>MyOtherClass</wordasword> with two instance
								variables.
								One will store a <code>String</code>, the other will store a <code>Dog</code>.
								Create
								getter and setter for these variables.
							</entry>
							<entry>
								<para>
									<programlisting language="java">
										<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
											parse="text" href="./examples/cheatsheets/MyOtherClass.java" />
									</programlisting>
								</para>
							</entry>
						</row>

					</tbody>
				</tgroup>
			</table>
		</para>
	</section>

	<section id="cheatsheets_localvariable">
		<title>Working with local variable</title>
		<para>A local variable must always be declared in a method.</para>
		<para>
			<table frame='all'>
				<tgroup cols='2' align='left' colsep='1' rowsep='1'>
					<colspec colname='c1' colwidth="1*"/>
					<colspec colname='c2' colwidth="2*"/>
					<thead>
						<row>
							<entry>What to do </entry>
							<entry>How to do it</entry>
						</row>
					</thead>
					<tbody>
						<row>
							<entry>Declare a (local) variable of type <code>String</code>.</entry>
							<entry><code>String variable1;</code></entry>
						</row>
						<row>
							<entry>
								Declare a (local) variable of type <code>String</code> and
								assign
								<code>"Test"</code>
								to it.
							</entry>
							<entry><code>String variable2 = "Test";</code></entry>
						</row>
						<row>
							<entry>Declare a (local) variable of type <code>Person</code></entry>
							<entry><code>Person person;</code></entry>
						</row>
						<row>
							<entry>Declare a (local) variable of type <code>Person</code>,
								create a new Object and assign the variable to this object.
							</entry>
							<entry><code>Person person = new Person();</code></entry>
						</row>
						<row>
							<entry>Declare an array of type <code>String</code></entry>
							<entry><code>String array[];</code></entry>
						</row>
						<row>
							<entry>
								Declare an array of type <code>Person</code> and create an array
								for this
								variable which can hold 5 persons.
							</entry>
							<entry><code>Person array[]= new Person[5];</code></entry>
						</row>
						<row>
							<entry>
								Assign 5 to the <code>int</code> variable <code>var1</code>
								(which was already declared);
							</entry>
							<entry><code>var1 = 5;</code></entry>
						</row>
						<row>
							<entry>
								Assign the existing variable <code>pers2</code> to the existing
								variable
								<code>pers1</code>;
							</entry>
							<entry><code>pers1 = pers2;</code></entry>
						</row>
						<row>
							<entry>
								Declare an <code>ArrayList</code> variable which can hold objects
								of type
								<code>Person</code>
							</entry>
							<entry>
								<![CDATA[ ArrayList<Person> persons;]]>
							</entry>
						</row>
						<row>
							<entry>
								Create a new <code>ArrayList</code>
								with objects of type <code>Person</code>
								and
								assign it to the existing variable <code>persons</code>.
							</entry>
							<entry>
								<![CDATA[ persons = new ArrayList<Person>();]]>
							</entry>
						</row>
						<row>
							<entry>
								Declare an <code>ArrayList</code> variable which can hold objects
								of type <code>Person</code>
								and create a new <code>Object</code> for it.
							</entry>
							<entry>
								<![CDATA[ ArrayList<Person> persons = new ArrayList<Person>();]]>
							</entry>
						</row>
					</tbody>
				</tgroup>
			</table>
		</para>
	</section>
</section>
